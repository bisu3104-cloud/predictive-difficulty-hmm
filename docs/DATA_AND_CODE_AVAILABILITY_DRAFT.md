# Data and Code Availability — release text

The selected HMM parameters, run-level neural and count-predictor results, cohort-selection records, frozen prediction outputs, and scripts required to reproduce the reported analyses are available in the GitHub repository `https://github.com/bisu3104-cloud/predictive-difficulty-hmm` and are archived on Zenodo at DOI `10.5281/zenodo.22004946`. The archive includes transition matrices, emission matrices, stationary distributions, deterministic generation metadata, and selection locks for the development, confirmatory, predictor-specific, strict-replication, external-generator, and dataset-realization robustness analyses. It also includes the run-level and aggregate CSV files used for the tables and figures.

For the dataset-realization robustness experiment, the archive includes the outcome-blind 16-HMM selection, public protocol, all 1,152 run-level results, split seeds, dataset checksums, and scripts that reconstruct the reported rank-stability, ICC, PCA, and frozen-prediction results. The regenerated sequence arrays and large neural checkpoints are not included because they are not required for reproducing the statistical analyses; dataset replicates 1 and 2 can be regenerated from the included HMM parameters and recorded seeds. The original replicate-0 sequence cache was unavailable, but its completed run summaries are included.

The archive also includes standalone reconstructions of the nested leave-one-HMM-out composite analysis, external PLS transfer, seed-aware variance-component analysis, and dataset-realization robustness analysis. Software under `code/` is licensed under the MIT License; data, derived results, figures, and documentation under `data/`, `outputs/`, and `docs/` are licensed under Creative Commons Attribution 4.0 International (CC BY 4.0).
